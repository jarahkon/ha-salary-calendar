name: Hassfest Validation

# Disabled while repo is private – re-enable triggers when public
on:
  workflow_dispatch:

permissions: {}

jobs:
  validate-hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Hassfest validation
        uses: home-assistant/actions/hassfest@master
